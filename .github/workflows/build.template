name: Build $NAME RMK firmware
on:
  workflow_dispatch:
  push:
    paths:
      - "**"

jobs:
  build:
    uses: haobogu/rmk/.github/workflows/user_build.yml@main
    with:
      keyboard_toml_path: "$NAME/keyboard.toml"
      vial_json_path: "$NAME/vial.json"
